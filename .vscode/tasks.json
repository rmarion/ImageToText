{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build tests",
            "type": "shell",
            "command": "make",
            "dependsOn": "link tests",
            "options": {"cwd": "${workspaceFolder}/CImageToText"}
        },
        {
            "label": "link tests",
            "type": "shell",
            "command": "cmake",
            "args": [
                "."
            ],
            "options": {"cwd": "${workspaceFolder}/CImageToText"}
        }

   ]
}